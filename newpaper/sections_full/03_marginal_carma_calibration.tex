\section{Marginal CARMA Calibration}\label{sec:marginal-carma-calibration}

\subsection{German Spot Prices}\label{ssec:german-price-carma-calibration}

The deseasonalized German log-price factor \(\widehat X^P\) is modeled by a
stationary CARMA process. In state-space form,
\begin{equation}
  dG_t = A G_t\,dt + e_p\,dL_t,
  \qquad
  X_t = b^\top G_t ,
  \label{eq:price-carma-state-space}
\end{equation}
where \(L\) is a one-dimensional Levy driver, \(A\) is the companion matrix of
the autoregressive polynomial
\begin{equation}
  a(z)=z^p+a_1z^{p-1}+\cdots+a_p,
  \label{eq:price-ar-polynomial}
\end{equation}
and \(b=(b_0,\ldots,b_q,0,\ldots,0)^\top\) contains the moving-average
coefficients. Stability requires the roots of \(a\) to have negative real
parts. For a fixed AR structure, the theoretical autocorrelation is a linear
combination of exponentials generated by these roots. Real roots generate
monotone exponential decay, while a complex conjugate pair generates a damped
oscillation.

\paragraph{ACF-based multiscale initialization.}
The CARMA order is selected from the empirical autocorrelation of
\(\widehat X^P\). The retained ACF model is
\begin{equation}
  \rho_{\mathrm{ms}}(h)
  =
  \sum_{i=1}^{3} w_i \exp(-\kappa_i h)
  +
  w_4 \exp(-\kappa_4 h)
  \cos\left(\frac{2\pi h}{24}\right),
  \qquad
  \sum_{i=1}^{4}w_i=1,\quad w_i\ge 0,
  \label{eq:price-multiscale-acf}
\end{equation}
with \(h\) measured in hours. The cosine frequency is fixed at
\(2\pi/24\), since the empirical ACF exhibits a clear 24-hour oscillatory
component. The three real modes represent different persistence scales: a fast
component of a few hours, an intermediate component of about one day, and a
slow component. The slow half-life is fixed at 120 days at this stage in order
to impose a long-memory component needed for option pricing horizons. This
constraint is used only to build a stable and interpretable initial CARMA
structure; the slow real half-life is re-estimated in the QMLE step.

The fitted multiscale decomposition has three real roots and one complex
conjugate pair. Therefore the autoregressive order is
\[
  p = 3 + 2 = 5 .
\]
We use the canonical full moving-average order \(q=p-1=4\), and the spectral
factorization of the fitted ACF gives a stable and invertible initial
CARMA(5,4) model.

\FloatBarrier

\begin{table}[!htbp]
  \centering
  \caption{German price CARMA(5,4) parameters from the ACF initialization.}
  \label{tab:price-carma-acf-init}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Quantity & Value & HL h & HL d & Status \\
    \midrule
    Log-likelihood & \(77561.686\) & & & before QMLE \\
    Levy drift \(m\) & \(1.396680{\times}10^{-5}\) & & & profiled \\
    Levy variance \(\nu^2\) & \(1.879996{\times}10^{-4}\) & & & profiled \\
    \midrule
    \(\lambda_f\) fast real & \(-0.263460\) & \(2.63\) & \(0.110\) & free in ACF fit \\
    \(\lambda_m\) intermediate real & \(-0.024161\) & \(28.69\) & \(1.195\) & free in ACF fit \\
    \(\lambda_s\) slow real & \(-2.4068{\times}10^{-4}\) & \(2880.00\) & \(120.00\) & fixed in ACF fit \\
    \(\lambda_d^\pm\) daily pair & \(-0.004004\pm0.261799i\) & \(173.11\) & \(7.213\) & period fixed \\
    Daily period & \(24.00\) h & & & fixed \\
    \midrule
    \(a_1\) & \(2.958700{\times}10^{-1}\) & & & implied by roots \\
    \(a_2\) & \(7.729482{\times}10^{-2}\) & & & implied by roots \\
    \(a_3\) & \(1.978743{\times}10^{-2}\) & & & implied by roots \\
    \(a_4\) & \(4.411413{\times}10^{-4}\) & & & implied by roots \\
    \(a_5\) & \(1.050270{\times}10^{-7}\) & & & implied by roots \\
    \midrule
    \(b_0\) & \(7.835423{\times}10^{-6}\) & & & spectral factorization \\
    \(b_1\) & \(7.187126{\times}10^{-3}\) & & & spectral factorization \\
    \(b_2\) & \(7.131652{\times}10^{-2}\) & & & spectral factorization \\
    \(b_3\) & \(1.304485{\times}10^{-1}\) & & & spectral factorization \\
    \(b_4\) & \(1.000000\) & & & normalization \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{German price CARMA(5,4) parameters after joint QMLE.}
  \label{tab:price-carma-after-qmle}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Quantity & Value & HL h & HL d & Status \\
    \midrule
    Log-likelihood & \(77709.048\) & & & after QMLE \\
    Levy drift \(m\) & \(8.473621{\times}10^{-6}\) & & & profiled \\
    Levy variance \(\nu^2\) & \(1.753615{\times}10^{-4}\) & & & profiled \\
    \midrule
    \(\lambda_f\) fast real & \(-0.153231\) & \(4.52\) & \(0.188\) & re-estimated \\
    \(\lambda_m\) intermediate real & \(-0.019289\) & \(35.94\) & \(1.497\) & re-estimated \\
    \(\lambda_s\) slow real & \(-3.5066{\times}10^{-4}\) & \(1976.67\) & \(82.36\) & re-estimated \\
    \(\lambda_d^\pm\) daily pair & \(-0.004004\pm0.261799i\) & \(173.11\) & \(7.213\) & fixed from ACF step \\
    Daily period & \(24.00\) h & & & fixed \\
    \midrule
    \(a_1\) & \(1.808780{\times}10^{-1}\) & & & implied by roots \\
    \(a_2\) & \(7.295539{\times}10^{-2}\) & & & implied by roots \\
    \(a_3\) & \(1.187629{\times}10^{-2}\) & & & implied by roots \\
    \(a_4\) & \(2.067768{\times}10^{-4}\) & & & implied by roots \\
    \(a_5\) & \(7.105209{\times}10^{-8}\) & & & implied by roots \\
    \midrule
    \(b_0\) & \(4.892285{\times}10^{-6}\) & & & re-estimated \\
    \(b_1\) & \(3.871538{\times}10^{-3}\) & & & re-estimated \\
    \(b_2\) & \(6.724760{\times}10^{-2}\) & & & re-estimated \\
    \(b_3\) & \(8.086069{\times}10^{-2}\) & & & re-estimated \\
    \(b_4\) & \(1.000000\) & & & normalization \\
    \bottomrule
  \end{tabular}
\end{table}

\FloatBarrier

The fixed daily pair fixes its quadratic AR factor. The reported coefficients
\(a_1,\ldots,a_5\) are nevertheless the coefficients of the full AR polynomial,
obtained after multiplying this fixed factor by the three real-root factors.

\Cref{fig:price-spot-acf-calibration} compares the empirical ACF with the
multiscale initialization and with the final QMLE CARMA ACF over 700 hourly
lags. The initialization captures both the fast decay and the persistent
daily oscillations. The QMLE step preserves this structure while adapting the
real half-lives and the moving-average polynomial to the exact likelihood.
Compared with the weather and renewable marginals below, the price model is the
only one that keeps three real modes in addition to the daily pair. The extra
slow mode is not introduced to improve short-lag fit; it carries persistence at
option-pricing horizons, where a few-day CARMA memory would be too short.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.97\textwidth]{calibration/price_spot_acf_calibration_700.pdf}
  \caption{German price ACF calibration over 700 hourly lags, comparing the
  empirical autocorrelation with the multiscale initialization and with the final
  CARMA(5,4) autocorrelation after QMLE.}
  \label{fig:price-spot-acf-calibration}
\end{figure}

\paragraph{Exact Gaussian QMLE.}
Starting from the ACF-based CARMA(5,4), the final marginal parameters are
estimated by exact Gaussian prediction-error QMLE. For given CARMA coefficients,
the Kalman recursion gives one-step prediction errors \(e_i\) and innovation
variances \(r_i\) under unit Levy variance. Since the Levy drift enters as a
constant mean shift, the adjusted innovation can be written
\[
  \varepsilon_i(m)=e_i-mc_i .
\]
The drift rate is profiled by weighted least squares,
\begin{equation}
  \widehat m
  =
  \frac{\sum_i e_i c_i/r_i}{\sum_i c_i^2/r_i},
  \qquad
  \widehat \nu^2
  =
  \frac{1}{n}\sum_i
  \frac{\varepsilon_i(\widehat m)^2}{r_i}.
  \label{eq:price-qmle-profile}
\end{equation}
The numerical objective is the reduced Gaussian prediction-error likelihood
augmented by a soft ACF penalty,
\begin{equation}
  \mathcal J(\theta)
  =
  \log(\widehat\nu^2)
  +
  \frac{1}{n}\sum_i \log r_i
  +
  \lambda_{\mathrm{acf}}
  \frac{1}{|H|}
  \sum_{h\in H}
  \omega_h
  \left(\rho_\theta(h)-\widehat\rho(h)\right)^2 .
  \label{eq:price-qmle-objective}
\end{equation}
The ACF penalty stabilizes the long-lag dependence without replacing the
likelihood criterion. In the German price calibration,
\(\lambda_{\mathrm{acf}}=10\), the final log-likelihood is \(77709.05\), and
the profiled Levy variance rate is \(\widehat\nu^2=1.7536\times10^{-4}\).

\begin{table}[!htbp]
  \centering
  \caption{Discrete-time ARMA log-likelihood benchmarks on the same
  deseasonalized German log-price residuals. These benchmarks are not used for
  pricing.}
  \label{tab:price-loglik-comparison}
  \small
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{@{}lcrp{0.43\textwidth}@{}}
    \toprule
    Model & Estimation & Log-likelihood & Comment \\
    \midrule
    ARMA(1,0) & discrete-time ML & \(77454.051\)
    & stationary benchmark \\
    ARMA(5,4) & discrete-time state-space ML & \(78660.514\)
    & LBFGS convergence flag not satisfied \\
    \bottomrule
  \end{tabular}
  }
\end{table}

The ARMA likelihoods are reported only as empirical benchmarks on the same
hourly residual series. They are not directly comparable as pricing models,
since they do not define the continuous-time CARMA dynamics used below for
simulation and option valuation.

\paragraph{Driver distribution.}
After the CARMA parameters are fixed, hourly increments of the latent Levy
driver are recovered from the smoothed state. We fit both a Gaussian law and a
normal inverse Gaussian law to these increments. The fitted parameters and
moments are reported in \Cref{tab:price-driver-distribution}.

\begin{table}[!htbp]
  \centering
  \caption{Fitted moments for recovered hourly Levy increments in the German
  price CARMA model.}
  \label{tab:price-driver-distribution}
  \begin{tabular}{lrrrr}
    \toprule
    Distribution & Mean & Std. dev. & Skewness & Excess kurtosis \\
    \midrule
    Gaussian & \(1.8906{\times}10^{-6}\) & \(1.3236{\times}10^{-2}\)
    & \(0.000\) & \(0.000\) \\
    NIG & \(1.8879{\times}10^{-6}\) & \(1.2135{\times}10^{-2}\)
    & \(-0.563\) & \(9.675\) \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[H]
  \centering
  \caption{Distribution parameters for the recovered German price driver.}
  \label{tab:price-driver-parameters}
  \begin{tabular}{llr}
    \toprule
    Distribution & Parameter & Value \\
    \midrule
    Gaussian & \(\mu\) & \(1.8906{\times}10^{-6}\) \\
    Gaussian & \(\sigma\) & \(1.3236{\times}10^{-2}\) \\
    NIG & \(\alpha\) & \(47.467\) \\
    NIG & \(\beta\) & \(-5.074\) \\
    NIG & \(\delta\) & \(6.870{\times}10^{-3}\) \\
    NIG & \(\mu\) & \(7.405{\times}10^{-4}\) \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{calibration/price_spot_driver_distribution_fit.pdf}
  \caption{Recovered hourly Levy driver increments for the German price CARMA
  model. The Gaussian fit underestimates the concentration around zero and the
  tail thickness, while the NIG fit gives the retained marginal driver law.}
  \label{fig:price-spot-driver-distribution}
\end{figure}

\Cref{fig:price-spot-distribution-checks} shows the effect of this driver law
at three levels: the deseasonalized log-price residual, the final shifted
log-price, and the final price in EUR/MWh. The same seasonal component is used
when mapping simulated CARMA residuals back to log-prices and prices.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.97\textwidth]{calibration/price_spot_distribution_checks.pdf}
  \caption{Distribution checks for the German price marginal model at the
  residual factor, shifted log-price, and EUR/MWh price levels.}
  \label{fig:price-spot-distribution-checks}
\end{figure}

The CARMA residual model improves the continuous stochastic component but does
not reproduce the bimodal empirical distribution of final prices, in particular
the concentration around zero EUR/MWh. That feature is induced by the market
price formation mechanism and is not captured by a smooth continuous-time
CARMA marginal factor alone.
The price driver is heavy-tailed, but its excess kurtosis is lower than for
solar and wind. The remaining mismatch therefore comes less from the Levy driver
alone than from the discontinuous empirical shape of final power prices.

\paragraph{Pure QMLE comparison.}
We also ran a pure exact QMLE without the ACF penalty
\((\lambda_{\mathrm{acf}}=0)\), using semi-global restarts from the same
CARMA(5,4) structure. The best run over 20 restarts increases the Gaussian
log-likelihood because the objective only rewards one-step prediction fit; it
does not penalize the loss of long-lag ACF structure.

\begin{table}[H]
  \centering
  \caption{Penalized versus pure QMLE for the German price CARMA(5,4).}
  \label{tab:price-pure-qmle-comparison}
  \begin{tabular}{@{}lcc@{}}
    \toprule
    Quantity & Retained QMLE & Pure QMLE \\
    \midrule
    ACF penalty \(\lambda_{\mathrm{acf}}\) & \(10\) & \(0\) \\
    Log-likelihood & \(77709.05\) & \(78808.20\) \\
    Fast real half-life & \(4.52\) h & \(0.51\) h \\
    Intermediate real half-life & \(35.94\) h & \(1.16\) h \\
    Slow real half-life & \(82.36\) d & \(1.36\) d \\
    Daily-pair half-life & \(7.21\) d & \(5.21\) d \\
    Daily period & \(24.00\) h & \(24.00\) h \\
    \bottomrule
  \end{tabular}
\end{table}

The pure QMLE is therefore better as a one-step Gaussian likelihood fit, but it
collapses the slow price factor from several months to about one day. This is
not appropriate for the pricing step, because the model would lose the
long-horizon persistence imposed by the ACF initialization and retained by the
penalized QMLE.

\FloatBarrier

\subsection{German Temperature}\label{ssec:german-temperature-carma-calibration}

The stochastic temperature factor \(\widehat X^T\) is the hourly Berlin
temperature residual after deterministic Fourier deseasonalization. It is
modeled with the same stationary CARMA state-space structure as in
\cref{eq:price-carma-state-space}. The empirical ACF is smoother than the
electricity price ACF, but still contains a daily oscillatory component. The
initial ACF model is therefore
\begin{equation}
  \rho_{\mathrm{ms}}^T(h)
  =
  w_f e^{-\kappa_f h}
  +
  w_s e^{-\kappa_s h}
  +
  w_d e^{-\kappa_d h}
  \cos\left(\frac{2\pi h}{24}\right),
  \qquad
  w_f+w_s+w_d=1,\quad w_f,w_s,w_d\ge 0 .
  \label{eq:temperature-multiscale-acf}
\end{equation}
The two real components represent a relatively fast decay and a slower
persistent component. The complex conjugate pair enforces the 24-hour
oscillation. This gives two real roots and one complex pair, hence
\[
  p = 2 + 2 = 4,
  \qquad q=p-1=3 .
\]
The daily frequency is fixed in the ACF step. In the subsequent QMLE step, the
two real AR roots and the moving-average polynomial are re-estimated, while the
daily AR pair is kept fixed to preserve the interpreted oscillatory component.

\FloatBarrier

\begin{table}[!htbp]
  \centering
  \caption{German temperature CARMA(4,3) parameters from the ACF initialization.}
  \label{tab:temperature-carma-acf-init}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Quantity & Value & HL h & HL d & Status \\
    \midrule
    Log-likelihood & \(-23759.581\) & & & before QMLE \\
    Levy drift \(m\) & \(5.479170{\times}10^{-4}\) & & & profiled \\
    Levy variance \(\nu^2\) & \(3.497107{\times}10^{-1}\) & & & profiled \\
    \midrule
    \(\lambda_f\) fast real & \(-0.027210\) & \(25.47\) & \(1.061\) & free in ACF fit \\
    \(\lambda_s\) slow real & \(-0.011917\) & \(58.17\) & \(2.424\) & free in ACF fit \\
    \(\lambda_d^\pm\) daily pair & \(-0.009532\pm0.261799i\) & \(72.72\) & \(3.030\) & period fixed \\
    Daily period & \(24.00\) h & & & fixed \\
    \midrule
    \(a_1\) & \(5.818961{\times}10^{-2}\) & & & implied by roots \\
    \(a_2\) & \(6.969989{\times}10^{-2}\) & & & implied by roots \\
    \(a_3\) & \(2.691419{\times}10^{-3}\) & & & implied by roots \\
    \(a_4\) & \(2.225311{\times}10^{-5}\) & & & implied by roots \\
    \midrule
    \(b_0\) & \(1.363967{\times}10^{-3}\) & & & spectral factorization \\
    \(b_1\) & \(6.904682{\times}10^{-2}\) & & & spectral factorization \\
    \(b_2\) & \(7.815313{\times}10^{-2}\) & & & spectral factorization \\
    \(b_3\) & \(1.000000\) & & & normalization \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{German temperature CARMA(4,3) parameters after joint QMLE.}
  \label{tab:temperature-carma-after-qmle}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Quantity & Value & HL h & HL d & Status \\
    \midrule
    Log-likelihood & \(-22940.645\) & & & after QMLE \\
    Levy drift \(m\) & \(2.853529{\times}10^{-4}\) & & & profiled \\
    Levy variance \(\nu^2\) & \(1.606579{\times}10^{-1}\) & & & profiled \\
    \midrule
    \(\lambda_f\) fast real & \(-1.308587\) & \(0.53\) & \(0.022\) & re-estimated \\
    \(\lambda_s\) slow real & \(-0.018198\) & \(38.09\) & \(1.587\) & re-estimated \\
    \(\lambda_d^\pm\) daily pair & \(-0.009532\pm0.261799i\) & \(72.72\) & \(3.030\) & fixed from ACF step \\
    Daily period & \(24.00\) h & & & fixed \\
    \midrule
    \(a_1\) & \(1.345849\) & & & implied by roots \\
    \(a_2\) & \(1.177365{\times}10^{-1}\) & & & implied by roots \\
    \(a_3\) & \(9.151094{\times}10^{-2}\) & & & implied by roots \\
    \(a_4\) & \(1.634346{\times}10^{-3}\) & & & implied by roots \\
    \midrule
    \(b_0\) & \(1.633022{\times}10^{-1}\) & & & re-estimated \\
    \(b_1\) & \(2.010868{\times}10^{-1}\) & & & re-estimated \\
    \(b_2\) & \(2.567675\) & & & re-estimated \\
    \(b_3\) & \(1.000000\) & & & normalization \\
    \bottomrule
  \end{tabular}
\end{table}

\FloatBarrier

The fixed daily AR pair fixes its quadratic factor. The full AR coefficients
\(a_1,\ldots,a_4\) are reported after multiplying this fixed factor by the two
real-root factors, so they change when the real roots are re-estimated.

\Cref{fig:temperature-acf-calibration} compares the empirical temperature ACF
with the multiscale initialization and the final QMLE CARMA ACF over 700 hourly
lags. The QMLE step shortens the fast real scale while preserving the daily
oscillatory structure and the slower decay.
After QMLE, the slow real half-life is about one and a half days, close to the
solar value and much shorter than the wind and price slow modes. Temperature is
therefore the cleanest two-scale physical marginal: a very fast adjustment, a
one-to-two-day persistence scale, and the fixed daily oscillation.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.97\textwidth]{calibration/temperature_acf_calibration_700.pdf}
  \caption{German temperature ACF calibration over 700 hourly lags, comparing
  the empirical autocorrelation with the multiscale initialization and with the
  final CARMA(4,3) autocorrelation after QMLE.}
  \label{fig:temperature-acf-calibration}
\end{figure}

\paragraph{Exact Gaussian QMLE and benchmarks.}
The same exact Gaussian prediction-error likelihood as in
\cref{eq:price-qmle-profile} is used for the temperature residual. The ACF
penalty is smaller than for prices, with \(\lambda_{\mathrm{acf}}=0.1\). The
final log-likelihood is \(-22940.65\), and the profiled Levy variance rate is
\(\widehat\nu^2=1.6066\times10^{-1}\).

\begin{table}[!htbp]
  \centering
  \caption{Discrete-time ARMA log-likelihood benchmarks on the same
  deseasonalized German temperature residuals. These benchmarks are not used for
  pricing.}
  \label{tab:temperature-loglik-comparison}
  \begin{tabular}{@{}lcl@{}}
    \toprule
    Model & Estimation & Log-likelihood \\
    \midrule
    ARMA(1,0) & discrete-time ML & \(-24611.865\) \\
    ARMA(4,3) & discrete-time state-space ML & \(-23173.448\) \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Driver distribution.}
Hourly increments of the recovered temperature Levy driver are fitted with both
a Gaussian law and a normal inverse Gaussian law. The NIG law is retained
because it captures the negative skewness and excess kurtosis of the recovered
increments.

\begin{table}[!htbp]
  \centering
  \caption{Fitted moments for recovered hourly Levy increments in the German
  temperature CARMA model.}
  \label{tab:temperature-driver-distribution}
  \begin{tabular}{lrrrr}
    \toprule
    Distribution & Mean & Std. dev. & Skewness & Excess kurtosis \\
    \midrule
    Gaussian & \(-2.5832{\times}10^{-4}\) & \(3.9309{\times}10^{-1}\)
    & \(0.000\) & \(0.000\) \\
    NIG & \(-2.5832{\times}10^{-4}\) & \(3.8501{\times}10^{-1}\)
    & \(-0.477\) & \(5.205\) \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[H]
  \centering
  \caption{Distribution parameters for the recovered German temperature driver.}
  \label{tab:temperature-driver-parameters}
  \begin{tabular}{llr}
    \toprule
    Distribution & Parameter & Value \\
    \midrule
    Gaussian & \(\mu\) & \(-2.5832{\times}10^{-4}\) \\
    Gaussian & \(\sigma\) & \(3.9309{\times}10^{-1}\) \\
    NIG & \(\alpha\) & \(2.064\) \\
    NIG & \(\beta\) & \(-0.257\) \\
    NIG & \(\delta\) & \(2.9888{\times}10^{-1}\) \\
    NIG & \(\mu\) & \(3.7243{\times}10^{-2}\) \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{calibration/temperature_driver_distribution_fit.pdf}
  \caption{Recovered hourly Levy driver increments for the German temperature
  CARMA model. The NIG fit captures the asymmetric and heavy-tailed driver
  distribution better than the Gaussian fit.}
  \label{fig:temperature-driver-distribution}
\end{figure}

\FloatBarrier

\Cref{fig:temperature-distribution-checks} reports the induced distributions at
the residual, residual-increment, and physical temperature levels. The same
Fourier seasonality is added back when simulated residuals are mapped to
degrees Celsius.
Unlike solar and wind, this final mapping is additive and unbounded. As a
result, distributional errors in the CARMA residual are not amplified or
compressed by a logit-type inverse transform.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.97\textwidth]{calibration/temperature_distribution_checks.pdf}
  \caption{Distribution checks for the German temperature marginal model at the
  deseasonalized residual, hourly residual-increment, and physical temperature
  levels.}
  \label{fig:temperature-distribution-checks}
\end{figure}

\FloatBarrier

\subsection{German Solar Production}\label{ssec:german-solar-carma-calibration}

The solar marginal model is fitted to the deseasonalized latent residual
\(\widehat X^Q\) defined after the clear-sky normalization and logit
transformation in \cref{ssec:solar-deseasonalization}. The physical capacity
factor is bounded and has structural nighttime zeros, so the CARMA model is not
applied directly to \(Q_t\). It is applied to the transformed residual, and
simulated paths are mapped back through the inverse clear-sky transformation.

The empirical ACF of \(\widehat X^Q\) has a sharp short-lag decay, a slower
component, and a residual daily oscillation. The ACF initialization therefore
uses the same two-real-plus-one-complex structure as temperature:
\begin{equation}
  \rho_{\mathrm{ms}}^Q(h)
  =
  w_f e^{-\kappa_f h}
  +
  w_s e^{-\kappa_s h}
  +
  w_d e^{-\kappa_d h}
  \cos\left(\frac{2\pi h}{24}\right),
  \qquad
  w_f+w_s+w_d=1,\quad w_f,w_s,w_d\ge 0 .
  \label{eq:solar-multiscale-acf}
\end{equation}
The fast real component captures sub-hour to intraday adjustment in the latent
cloudiness factor, the slower real component captures persistence over roughly
one to two days, and the complex pair imposes the 24-hour oscillatory mode. This
gives a CARMA(4,3) specification.

\FloatBarrier

\begin{table}[!htbp]
  \centering
  \caption{German solar CARMA(4,3) parameters from the ACF initialization.}
  \label{tab:solar-carma-acf-init}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Quantity & Value & HL h & HL d & Status \\
    \midrule
    Log-likelihood & \(-84636.518\) & & & before QMLE \\
    Levy drift \(m\) & \(1.136470{\times}10^{-4}\) & & & profiled \\
    Levy variance \(\nu^2\) & \(9.270238\) & & & profiled \\
    \midrule
    \(\lambda_f\) fast real & \(-1.370835\) & \(0.51\) & \(0.021\) & free in ACF fit \\
    \(\lambda_s\) slow real & \(-0.016625\) & \(41.69\) & \(1.737\) & free in ACF fit \\
    \(\lambda_d^\pm\) daily pair & \(-0.009845\pm0.261799i\) & \(70.41\) & \(2.934\) & period fixed \\
    Daily period & \(24.00\) h & & & fixed \\
    \midrule
    \(a_1\) & \(1.407149\) & & & implied by roots \\
    \(a_2\) & \(1.187446{\times}10^{-1}\) & & & implied by roots \\
    \(a_3\) & \(9.567819{\times}10^{-2}\) & & & implied by roots \\
    \(a_4\) & \(1.564222{\times}10^{-3}\) & & & implied by roots \\
    \midrule
    \(b_0\) & \(8.477053{\times}10^{-3}\) & & & spectral factorization \\
    \(b_1\) & \(8.361212{\times}10^{-2}\) & & & spectral factorization \\
    \(b_2\) & \(2.305833{\times}10^{-1}\) & & & spectral factorization \\
    \(b_3\) & \(1.000000\) & & & normalization \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{German solar CARMA(4,3) parameters after joint QMLE.}
  \label{tab:solar-carma-after-qmle}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Quantity & Value & HL h & HL d & Status \\
    \midrule
    Log-likelihood & \(-84622.936\) & & & after QMLE \\
    Levy drift \(m\) & \(1.269139{\times}10^{-4}\) & & & profiled \\
    Levy variance \(\nu^2\) & \(9.122430\) & & & profiled \\
    \midrule
    \(\lambda_f\) fast real & \(-1.353919\) & \(0.51\) & \(0.021\) & re-estimated \\
    \(\lambda_s\) slow real & \(-0.018410\) & \(37.65\) & \(1.569\) & re-estimated \\
    \(\lambda_d^\pm\) daily pair & \(-0.009845\pm0.261799i\) & \(70.41\) & \(2.934\) & fixed from ACF step \\
    Daily period & \(24.00\) h & & & fixed \\
    \midrule
    \(a_1\) & \(1.392018\) & & & implied by roots \\
    \(a_2\) & \(1.205815{\times}10^{-1}\) & & & implied by roots \\
    \(a_3\) & \(9.468167{\times}10^{-2}\) & & & implied by roots \\
    \(a_4\) & \(1.710750{\times}10^{-3}\) & & & implied by roots \\
    \midrule
    \(b_0\) & \(8.992189{\times}10^{-3}\) & & & re-estimated \\
    \(b_1\) & \(8.187720{\times}10^{-2}\) & & & re-estimated \\
    \(b_2\) & \(2.392272{\times}10^{-1}\) & & & re-estimated \\
    \(b_3\) & \(1.000000\) & & & normalization \\
    \bottomrule
  \end{tabular}
\end{table}

\FloatBarrier

The daily AR pair is fixed after the ACF step. The QMLE step therefore adjusts
the two real persistence scales and the MA polynomial while preserving the
interpreted 24-hour oscillation.

\Cref{fig:solar-acf-calibration} compares the empirical ACF with the multiscale
initialization and the final QMLE CARMA ACF over 700 hourly lags. The final fit
keeps the short-lag decay and the daily oscillatory structure, while the
remaining long-lag empirical dependence is only partially captured by this
four-dimensional marginal factor.
The solar real half-lives are close to the temperature half-lives after QMLE,
but their interpretation is different. The fast solar mode reflects cloudiness
in the clear-sky coordinate, while the daily component is tied to the production
support itself; wind below instead puts more weight on slow weather-regime
persistence.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.97\textwidth]{calibration/solar_acf_calibration_700.pdf}
  \caption{German solar latent-residual ACF calibration over 700 hourly lags,
  comparing the empirical autocorrelation with the multiscale initialization and
  with the final CARMA(4,3) autocorrelation after QMLE.}
  \label{fig:solar-acf-calibration}
\end{figure}

\paragraph{Exact Gaussian QMLE and benchmarks.}
The exact Gaussian prediction-error likelihood is applied to the latent residual
\(\widehat X^Q\). The ACF penalty is set to
\(\lambda_{\mathrm{acf}}=1000\), since the solar residual ACF contains a strong
daily component that must be preserved by the continuous-time marginal model.
The final log-likelihood is \(-84622.94\), and the profiled Levy variance rate
is \(\widehat\nu^2=9.1224\).

\begin{table}[!htbp]
  \centering
  \caption{Discrete-time ARMA log-likelihood benchmarks on the same
  deseasonalized German solar latent residuals. These benchmarks are not used
  for pricing.}
  \label{tab:solar-loglik-comparison}
  \begin{tabular}{@{}lcl@{}}
    \toprule
    Model & Estimation & Log-likelihood \\
    \midrule
    ARMA(1,0) & discrete-time ML & \(-86462.196\) \\
    ARMA(4,3) & discrete-time state-space ML & \(-84785.671\) \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Driver distribution.}
The recovered solar Levy increments are strongly non-Gaussian. The NIG law is
therefore retained because it captures the negative skewness and high excess
kurtosis that are absent from a Gaussian driver.
Among the four marginals, solar has the largest negative skewness and excess
kurtosis in the recovered driver. This is consistent with abrupt cloud-driven
changes in the latent coordinate and with the bounded physical output after the
inverse clear-sky transformation.

\begin{table}[!htbp]
  \centering
  \caption{Fitted moments for recovered hourly Levy increments in the German
  solar CARMA model.}
  \label{tab:solar-driver-distribution}
  \begin{tabular}{lrrrr}
    \toprule
    Distribution & Mean & Std. dev. & Skewness & Excess kurtosis \\
    \midrule
    Gaussian & \(1.3308{\times}10^{-4}\) & \(2.8964\)
    & \(0.000\) & \(0.000\) \\
    NIG & \(1.3310{\times}10^{-4}\) & \(3.0420\)
    & \(-0.803\) & \(18.789\) \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[H]
  \centering
  \caption{Distribution parameters for the recovered German solar driver.}
  \label{tab:solar-driver-parameters}
  \begin{tabular}{llr}
    \toprule
    Distribution & Parameter & Value \\
    \midrule
    Gaussian & \(\mu\) & \(1.3308{\times}10^{-4}\) \\
    Gaussian & \(\sigma\) & \(2.8964\) \\
    NIG & \(\alpha\) & \(1.3609{\times}10^{-1}\) \\
    NIG & \(\beta\) & \(-1.4892{\times}10^{-2}\) \\
    NIG & \(\delta\) & \(1.2368\) \\
    NIG & \(\mu\) & \(1.3629{\times}10^{-1}\) \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{calibration/solar_driver_distribution_fit.pdf}
  \caption{Recovered hourly Levy driver increments for the German solar CARMA
  model. The NIG fit captures the asymmetric and heavy-tailed driver
  distribution better than the Gaussian fit.}
  \label{fig:solar-driver-distribution}
\end{figure}

\FloatBarrier

\Cref{fig:solar-distribution-checks} reports the induced distributions at the
latent residual, residual-increment, and physical capacity-factor levels. The
physical mapping uses the same clear-sky envelope, fitted transform bounds, and
latent seasonal component as the deseasonalization step.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.97\textwidth]{calibration/solar_distribution_checks.pdf}
  \caption{Distribution checks for the German solar marginal model at the
  deseasonalized latent residual, hourly residual-increment, and physical
  capacity-factor levels.}
  \label{fig:solar-distribution-checks}
\end{figure}

The CARMA-NIG model improves the latent residual tails relative to a Gaussian
driver, but it does not fully reproduce the left-skewed empirical residual law.
Part of this asymmetry comes from the bounded clear-sky transformation and the
structural zero-production periods in physical solar output.

\FloatBarrier

\subsection{German Wind Production}\label{ssec:german-wind-carma-calibration}

The wind marginal model is fitted to the deseasonalized logit residual
\(\widehat X^W\) defined in \cref{ssec:wind-deseasonalization}. As for solar,
the CARMA model is estimated in an unconstrained latent coordinate and simulated
paths are mapped back to capacity factors with the inverse logit transform. This
keeps the continuous-time model away from the physical bounds \(0\) and \(1\).

The empirical wind ACF is less dominated by a deterministic day-night structure
than the solar ACF, but it still contains a weak daily oscillatory component and
several persistence scales. The ACF initialization uses
\begin{equation}
  \rho_{\mathrm{ms}}^W(h)
  =
  w_f e^{-\kappa_f h}
  +
  w_s e^{-\kappa_s h}
  +
  w_d e^{-\kappa_d h}
  \cos\left(\frac{2\pi h}{24}\right),
  \qquad
  w_f+w_s+w_d=1,\quad w_f,w_s,w_d\ge 0 .
  \label{eq:wind-multiscale-acf}
\end{equation}
The first real mode captures intraday persistence, the second real mode captures
slower weather-regime persistence, and the complex pair fixes the 24-hour
oscillation. This again gives a CARMA(4,3) specification.

\FloatBarrier

\begin{table}[!htbp]
  \centering
  \caption{German wind CARMA(4,3) parameters from the ACF initialization.}
  \label{tab:wind-carma-acf-init}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Quantity & Value & HL h & HL d & Status \\
    \midrule
    Log-likelihood & \(-7013.176\) & & & before QMLE \\
    Levy drift \(m\) & \(6.357371{\times}10^{-5}\) & & & profiled \\
    Levy variance \(\nu^2\) & \(1.015442{\times}10^{-1}\) & & & profiled \\
    \midrule
    \(\lambda_f\) fast real & \(-0.043126\) & \(16.07\) & \(0.670\) & free in ACF fit \\
    \(\lambda_s\) slow real & \(-0.003883\) & \(178.51\) & \(7.438\) & free in ACF fit \\
    \(\lambda_d^\pm\) daily pair & \(-0.008797\pm0.261799i\) & \(78.80\) & \(3.283\) & period fixed \\
    Daily period & \(24.00\) h & & & fixed \\
    \midrule
    \(a_1\) & \(6.460237{\times}10^{-2}\) & & & implied by roots \\
    \(a_2\) & \(6.961081{\times}10^{-2}\) & & & implied by roots \\
    \(a_3\) & \(3.228522{\times}10^{-3}\) & & & implied by roots \\
    \(a_4\) & \(1.149037{\times}10^{-5}\) & & & implied by roots \\
    \midrule
    \(b_0\) & \(4.273146{\times}10^{-4}\) & & & spectral factorization \\
    \(b_1\) & \(6.854653{\times}10^{-2}\) & & & spectral factorization \\
    \(b_2\) & \(4.721584{\times}10^{-2}\) & & & spectral factorization \\
    \(b_3\) & \(1.000000\) & & & normalization \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{German wind CARMA(4,3) parameters after joint QMLE.}
  \label{tab:wind-carma-after-qmle}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Quantity & Value & HL h & HL d & Status \\
    \midrule
    Log-likelihood & \(-6983.728\) & & & after QMLE \\
    Levy drift \(m\) & \(1.572834{\times}10^{-5}\) & & & profiled \\
    Levy variance \(\nu^2\) & \(9.934486{\times}10^{-2}\) & & & profiled \\
    \midrule
    \(\lambda_f\) fast real & \(-0.033136\) & \(20.92\) & \(0.872\) & re-estimated \\
    \(\lambda_s\) slow real & \(-7.6809{\times}10^{-4}\) & \(902.44\) & \(37.60\) & re-estimated \\
    \(\lambda_d^\pm\) daily pair & \(-0.008797\pm0.261799i\) & \(78.80\) & \(3.283\) & fixed from ACF step \\
    Daily period & \(24.00\) h & & & fixed \\
    \midrule
    \(a_1\) & \(5.149728{\times}10^{-2}\) & & & implied by roots \\
    \(a_2\) & \(6.923824{\times}10^{-2}\) & & & implied by roots \\
    \(a_3\) & \(2.326800{\times}10^{-3}\) & & & implied by roots \\
    \(a_4\) & \(1.746357{\times}10^{-6}\) & & & implied by roots \\
    \midrule
    \(b_0\) & \(9.228613{\times}10^{-5}\) & & & re-estimated \\
    \(b_1\) & \(6.888442{\times}10^{-2}\) & & & re-estimated \\
    \(b_2\) & \(5.391827{\times}10^{-2}\) & & & re-estimated \\
    \(b_3\) & \(1.000000\) & & & normalization \\
    \bottomrule
  \end{tabular}
\end{table}

\FloatBarrier

The QMLE step increases the slow real half-life to about 38 days. This mode is
not a calendar effect; it represents persistent wind-regime variation in the
latent residual. The daily AR pair remains fixed from the ACF step.
This is the main contrast with solar. Both renewable marginals use a bounded
latent transform, but wind keeps a much longer real persistence scale, whereas
solar is dominated by short-lag cloud effects and the deterministic day-night
structure.

\Cref{fig:wind-acf-calibration} compares the empirical ACF with the multiscale
initialization and the final QMLE CARMA ACF over 700 hourly lags. The fit
captures the main short-lag decay and the broad persistent component, while the
remaining small oscillations are absorbed through the fixed daily pair.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.97\textwidth]{calibration/wind_acf_calibration_700.pdf}
  \caption{German wind logit-residual ACF calibration over 700 hourly lags,
  comparing the empirical autocorrelation with the multiscale initialization and
  with the final CARMA(4,3) autocorrelation after QMLE.}
  \label{fig:wind-acf-calibration}
\end{figure}

\paragraph{Exact Gaussian QMLE and benchmarks.}
The exact Gaussian prediction-error likelihood is applied to the wind logit
residual \(\widehat X^W\), with \(\lambda_{\mathrm{acf}}=0.1\). The final
log-likelihood is \(-6983.73\), and the profiled Levy variance rate is
\(\widehat\nu^2=9.9345\times10^{-2}\).

\begin{table}[!htbp]
  \centering
  \caption{Discrete-time ARMA log-likelihood benchmarks on the same
  deseasonalized German wind logit residuals. These benchmarks are not used for
  pricing.}
  \label{tab:wind-loglik-comparison}
  \begin{tabular}{@{}lcl@{}}
    \toprule
    Model & Estimation & Log-likelihood \\
    \midrule
    ARMA(1,0) & discrete-time ML & \(-7261.653\) \\
    ARMA(4,3) & discrete-time state-space ML & \(-6853.455\) \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Driver distribution.}
The recovered wind Levy increments have a narrow center, negative skewness, and
large excess kurtosis. A NIG law is therefore retained for simulation.
Its excess kurtosis is close to the solar one, but the skewness is weaker. The
wind difficulty is therefore not only marginal tail thickness; it is also the
combination of heavy-tailed innovations with a slow real factor.

\begin{table}[!htbp]
  \centering
  \caption{Fitted moments for recovered hourly Levy increments in the German
  wind CARMA model.}
  \label{tab:wind-driver-distribution}
  \begin{tabular}{lrrrr}
    \toprule
    Distribution & Mean & Std. dev. & Skewness & Excess kurtosis \\
    \midrule
    Gaussian & \(-2.7840{\times}10^{-4}\) & \(3.1517{\times}10^{-1}\)
    & \(0.000\) & \(0.000\) \\
    NIG & \(-2.7839{\times}10^{-4}\) & \(2.9423{\times}10^{-1}\)
    & \(-0.370\) & \(15.002\) \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[H]
  \centering
  \caption{Distribution parameters for the recovered German wind driver.}
  \label{tab:wind-driver-parameters}
  \begin{tabular}{llr}
    \toprule
    Distribution & Parameter & Value \\
    \midrule
    Gaussian & \(\mu\) & \(-2.7840{\times}10^{-4}\) \\
    Gaussian & \(\sigma\) & \(3.1517{\times}10^{-1}\) \\
    NIG & \(\alpha\) & \(1.5339\) \\
    NIG & \(\beta\) & \(-8.5117{\times}10^{-2}\) \\
    NIG & \(\delta\) & \(1.3218{\times}10^{-1}\) \\
    NIG & \(\mu\) & \(7.0675{\times}10^{-3}\) \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{calibration/wind_driver_distribution_fit.pdf}
  \caption{Recovered hourly Levy driver increments for the German wind CARMA
  model. The NIG fit captures the narrow center and heavy tails better than the
  Gaussian fit.}
  \label{fig:wind-driver-distribution}
\end{figure}

\FloatBarrier

\Cref{fig:wind-distribution-checks} reports the induced distributions at the
latent residual, residual-increment, and physical capacity-factor levels. The
physical mapping adds the fitted logit seasonality and then applies the inverse
logit transform.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.97\textwidth]{calibration/wind_distribution_checks.pdf}
  \caption{Distribution checks for the German wind marginal model at the
  deseasonalized logit residual, hourly residual-increment, and physical
  capacity-factor levels.}
  \label{fig:wind-distribution-checks}
\end{figure}

The NIG driver improves the residual-increment tails relative to a Gaussian
driver, but the latent residual distribution remains more left-skewed in the
data than in the simulated CARMA paths. This limitation is most visible in the
logit coordinate and is partly compressed after mapping back to capacity
factors.

\FloatBarrier
